<package xmlns="http://expath.org/ns/pkg" spec="1.0" version="${project.version}" abbrev="${project.artifactId}"
         name="https://pkg.dmaus.name/expath-pkg/${project.artifactId}">

  <title>SchXslt2 Schematron to XSLT 3.0 transpiler</title>

  <xslt>
    <import-uri>https://pkg.dmaus.name/expath-pkg/${project.artifactId}/transpile.xsl</import-uri>
    <file>transpile.xsl</file>
  </xslt>

</package>
